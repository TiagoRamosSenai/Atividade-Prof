-- Criação do banco de dados
CREATE DATABASE IF NOT EXISTS saep_db;
USE saep_db;

-- Criação da tabela 'professores'
CREATE TABLE IF NOT EXISTS professores (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(100) NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL,
    senha VARCHAR(255) NOT NULL
);

-- Inserção de dados de teste na tabela 'professores'
INSERT INTO professores (nome, email, senha) VALUES
('Tiago Ramos', 'tiago@gmail.com', '123456'),  -- Substitua por um hash seguro para produção
('Maria Silva', 'maria@gmail.com', 'senha123');

-- Criação da tabela 'turmas'
CREATE TABLE IF NOT EXISTS turmas (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(100) NOT NULL,
    id_professor INT,
    FOREIGN KEY (id_professor) REFERENCES professores(id)
);

-- Criação da tabela 'atividades'
CREATE TABLE atividades (
    id INT AUTO_INCREMENT PRIMARY KEY,
    descricao VARCHAR(255) NOT NULL,
    id_turma INT NOT NULL,
    FOREIGN KEY (id_turma) REFERENCES turmas(id)
);


-- Verificação dos dados inseridos
SELECT * FROM professores;
SELECT * FROM turmas;
SELECT * FROM atividades;